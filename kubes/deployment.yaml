apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: aspnet
spec:
  replicas: 2
  selector:
    matchLabels:
      app: aspnet
  template:
    metadata:
      labels:
        app: aspnet
    spec:
      containers:
      - env:
        image: gcr.io/lloyd-test/aspnet:3b9ee83dfb4d99e761055a8c2d1a3f662b9ea77b
        name: aspnet
        ports:
        - name: payments-port
          containerPort: 80
        readinessProbe:
          httpGet:
            path: /home/about
            port: payments-port
            httpHeaders:
              - name: X-Requested-With
                value: ReadinessProbe
          initialDelaySeconds: 15
          periodSeconds: 60
          timeoutSeconds: 5
          failureThreshold: 1
          successThreshold: 1
status: {}
